/** Bookings list rhythm — vertical gap between stacked booking cards. */
export const BOOKING_CARD_GAP = 12;

/** Leading mode-icon circle on each booking card (matches section-header circle weight). */
export const BOOKING_CARD_LEADING_SIZE = 44;
