import { radii } from '@/theme/tokens';

/** Shared ritual chrome — matches `RitualPrimaryButton` + `radii.ritualLg`. */
export const RITUAL_CORNER_RADIUS = radii.ritualLg;

/** Home trending carousel — partial next card visible for quiet discoverability. */
export const HOME_RITUAL_CARD_PEEK_PX = 36;

/** Compact editorial card — image-first without marketplace proportions. */
export const HOME_RITUAL_CARD_ARTWORK_HEIGHT = 108;

/** Between carousel tiles — tactile but calm. */
export const HOME_RITUAL_CARD_GAP = 14;
